services:
  app:
    build:
      context: .
    restart: always
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 10
    container_name: chat-ai-backend
    ports:
      - "3002:3002"
    environment:
      - NODE_ENV=production
      - PORT=3002
